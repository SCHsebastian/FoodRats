// Compose-compiler stability configuration (read at compile time; no source change).
// Lists immutable read-models + designsystem List-param types the compiler should treat
// as stable so they don't force recomposition / unstable-lambda boxing. `//` = comment,
// `*` = single segment wildcard, `**` = any subpackage. Every FQN below is verified to
// resolve to a real declaration in this repo.

// Immutable read-models
es.schsebastian.foodrats.core.domain.account.Account
es.schsebastian.foodrats.feature.crew.domain.model.Crew
es.schsebastian.foodrats.feature.crew.domain.model.Member
es.schsebastian.foodrats.feature.crew.domain.model.JoinRequest
es.schsebastian.foodrats.feature.stats.domain.model.**
es.schsebastian.foodrats.feature.achievements.domain.model.**
es.schsebastian.foodrats.core.domain.meal.Ingredient

// Design-system structural List-param item types
es.schsebastian.foodrats.core.designsystem.structural.FrDockItem
es.schsebastian.foodrats.core.designsystem.structural.FrBentoItem

// External value types treated as stable (no @Stable annotation available on them)
kotlin.time.Instant
kotlinx.datetime.**
