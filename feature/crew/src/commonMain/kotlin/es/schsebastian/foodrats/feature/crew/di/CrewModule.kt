package es.schsebastian.foodrats.feature.crew.di

import org.koin.dsl.module

val crewModule = module {
    // Scaffold only. Add CrewRepository, use cases, and view models following
    // the :feature:meal pattern when the feature is implemented.
}
