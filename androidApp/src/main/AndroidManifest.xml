<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.location.network" android:required="false"/>

    <!-- Android 11+ package visibility: the share-card flow resolves the Instagram-Stories intent
         (com.instagram.share.ADD_TO_STORY) before launching it; without this <queries> entry
         resolveActivity always returns null and every share falls back to the system sheet.
         The generic ACTION_SEND fallback is implicitly visible. (Shareable story cards, spec §6.1.) -->
    <queries>
        <intent>
            <action android:name="com.instagram.share.ADD_TO_STORY"/>
        </intent>
        <package android:name="com.instagram.android"/>
    </queries>

    <application
        android:name=".FoodRatsApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        tools:targetApi="33">
        <activity android:name=".MainActivity"
                  android:exported="true"
                  android:launchMode="singleTop"
                  android:windowSoftInputMode="adjustResize"
                  android:theme="@style/Theme.FoodRats.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Verified App Links against the LIVE Firebase Hosting domain (foodrats-de4ec.web.app),
                 which actually serves /.well-known/assetlinks.json (deployed from website/). This is
                 the only host the app links against; it must stay in sync with DeepLinks.WEB_HOST
                 (shared/.../app/navigation/DeepLink.kt) and docs/store-release/PUBLICATION.md. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="foodrats-de4ec.web.app"/>
            </intent-filter>

            <!-- Custom-scheme fallback: foodrats://app/... (host is "app" so the route
                 discriminator stays in the path — see DeepLinks). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="foodrats" android:host="app"/>
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.FoodRatsFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Single FileProvider (the same component class can't be declared twice) with two
             authorities:
             - "${applicationId}.fileprovider": shareable story-card PNG (StoryShareLauncherAndroid);
               must match ShareCardFiles.fileProviderAuthority. Read permission is granted per-share
               via FLAG_GRANT_READ_URI_PERMISSION. (Spec §6.1.)
             - "${applicationId}.photopicker": native camera captures (rememberPhotoPicker →
               TakePicture); must match the authority built in PhotoPicker.android.kt. The system
               camera writes the capture through the granted content:// URI.
             Both expose only the app's own cache subdirs listed in @xml/file_paths. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider;${applicationId}.photopicker"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- FCM display defaults for *notification-type* messages the system shows while the app
             is backgrounded. Data messages (the server's meal-post/comment/reminder pushes) are
             handled in FoodRatsFirebaseMessagingService.onMessageReceived and never use these.
             default_notification_icon MUST be a monochrome silhouette — a full-color launcher icon
             renders as a white square in the status bar. The channel points at the existing
             "fr_nudge" channel (NotificationChannels.NUDGE_ID); add a dedicated general channel if
             the server ever starts sending notification-type messages. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="fr_nudge" />

        <!-- Privacy / consent defaults: GA4 starts with collection OFF and Consent Mode DENIED.
             The app flips both on only when the user grants consent
             (FirebaseAnalyticsTracker.applyConsent → setAnalyticsCollectionEnabled(true) +
             setConsent(ANALYTICS_STORAGE=GRANTED)). Matches the Play Data-Safety declaration
             that analytics is consent-gated. (human.md §D) -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_analytics_storage"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_storage"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_user_data"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />

    </application>
</manifest>
