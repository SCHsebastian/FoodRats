# Skip pnpm's pre-`run` dependency-status check: it shells out to `pnpm install`,
# which trips the build-approval gate and breaks `pnpm test`. Deps are installed
# explicitly via `pnpm install` before running the suite.
verify-deps-before-run=false
